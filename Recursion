#include<stdio.h>
int fib(int a)
{
  if a==1 || a==0
     return 1;
  else
  return fib(a-1)+fib(n-2);
}
int main()
{
int n,i;
printf("Enter Value of integer:\n");
scanf("%d",&n);
printf("Result :%d\n",fib(n));
}
